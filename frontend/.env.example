# URL of the Go gateway backend.
# In docker-compose this is http://gateway-go:8080
BACKEND_API_URL=http://localhost:8080
# Secure flag for the session cookie. Set to true when serving over HTTPS.
COOKIE_SECURE=false
